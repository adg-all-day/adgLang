{
  "order": 6,
  "title": "Loop Patterns",
  "snippet": "Simulating for-loops",
  "description": "ADGLANG does not have a dedicated C-style 'for' loop. Instead, you use the 'loop' keyword (which acts like 'while') to achieve the same result. This involves initializing a counter before the loop, checking the condition in the loop header, and incrementing the counter inside the loop body. This example demonstrates how to implement common iteration patterns.",
  "code": [
    "import [IO] from \"std/io.adg\";",
    "",
    "frame main() ret int {",
    "    # Basic iteration (0 to 9)",
    "    local i: int = 0;",
    "    IO.printString(\"Count 0 to 9:\");",
    "    loop (i < 10) {",
    "        IO.printf(\"%d \", i);",
    "        i = i + 1;",
    "    }",
    "    IO.printString(\"\");",
    "    ",
    "    # Iteration with step (0, 2, 4...)",
    "    local j: int = 0;",
    "    IO.printString(\"\\nEven numbers 0 to 10:\");",
    "    loop (j <= 10) {",
    "        IO.printf(\"%d \", j);",
    "        j = j + 2;",
    "    }",
    "    IO.printString(\"\");",
    "    ",
    "    # Nested loops",
    "    local row: int = 1;",
    "    local col: int;",
    "    IO.printString(\"\\nMultiplication table:\");",
    "    loop (row <= 5) {",
    "        col = 1;",
    "        loop (col <= 5) {",
    "            IO.printf(\"%3d \", row * col);",
    "            col = col + 1;",
    "        }",
    "        IO.printString(\"\");",
    "        row = row + 1;",
    "    }",
    "    ",
    "    return 0;",
    "}"
  ]
}
