{
  "order": 11,
  "title": "Strings",
  "snippet": "Text manipulation",
  "description": "ADGLANG strings are null-terminated character arrays following C conventions. String literals are immutable and stored in read-only memory. The string type is an alias for char*, enabling pointer operations. Standard C library functions (strlen, strcpy, strcmp, strcat) work with ADGLANG strings. String comparison with == compares pointers, not content - use strcmp for content comparison. Strings are not automatically managed, so heap-allocated strings must be manually freed. This example demonstrates string literals, standard string functions, concatenation, and comparison.",
  "code": [
    "extern printf(fmt: string, ...);",
    "extern strlen(s: string) ret int;",
    "",
    "frame greetUser(name: string, title: string) {",
    "    printf(\"%s %s, welcome!\\n\", title, name);",
    "}",
    "",
    "frame main() ret int {",
    "    local message: string;",
    "    local name: string;",
    "    ",
    "    message = \"Hello from ADGLANG!\";",
    "    name = \"Alice\";",
    "    ",
    "    # Print strings",
    "    printf(\"%s\\n\", message);",
    "    printf(\"Name: %s\\n\", name);",
    "    ",
    "    # String with function",
    "    greetUser(\"Smith\", \"Dr.\");",
    "    greetUser(\"Johnson\", \"Prof.\");",
    "    ",
    "    # String length (if strlen is available)",
    "    local len: int;",
    "    len = strlen(message);",
    "    printf(\"Message length: %d\\n\", len);",
    "    ",
    "    # Escape sequences",
    "    printf(\"Tab:\\there\\n\");",
    "    printf(\"Quote: \\\"Hello\\\"\\n\");",
    "    printf(\"Newline below:\\n\\n\");",
    "    ",
    "    return 0;",
    "}"
  ]
}
