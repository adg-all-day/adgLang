{
  "order": 5,
  "title": "While Loops",
  "snippet": "Loop with a condition",
  "description": "ADGLANG uses the 'loop' keyword instead of 'while'. A loop with a condition executes repeatedly as long as the condition evaluates to true. Use 'break' to exit a loop early and 'continue' to skip to the next iteration.",
  "code": [
    "import [IO] from \"std/io.adg\";",
    "",
    "frame main() ret int {",
    "    # Basic while loop",
    "    local count: int = 0;",
    "    ",
    "    IO.printString(\"Counting to 5:\");",
    "    loop (count < 5) {",
    "        count = count + 1;",
    "        IO.printf(\"%d \", count);",
    "    }",
    "    IO.printString(\"\");",
    "    ",
    "    # While with break",
    "    local i: int = 0;",
    "    IO.printString(\"\\nWith break at 3:\");",
    "    loop (i < 10) {",
    "        i = i + 1;",
    "        if (i == 3) {",
    "            break;",
    "        }",
    "        IO.printf(\"%d \", i);",
    "    }",
    "    IO.printString(\"\");",
    "    ",
    "    # While with continue",
    "    local j: int = 0;",
    "    IO.printString(\"\\nSkip even numbers:\");",
    "    loop (j < 10) {",
    "        j = j + 1;",
    "        if (j % 2 == 0) {",
    "            continue;",
    "        }",
    "        IO.printf(\"%d \", j);",
    "    }",
    "    IO.printString(\"\");",
    "    ",
    "    return 0;",
    "}"
  ]
}
