import [Log] from "std/log.adg";

frame main() ret int {
    Log.info("=== Log Demo ===");
    Log.debug("debug message");
    Log.warn("warn message");
    Log.error("error message");
    return 0;
}
